import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({

  logoContainer: {
    position: 'absolute',
    top: 100,
  },
  logo: {
    width: 100,
    height: 100,
  },

});
export default Styles;
